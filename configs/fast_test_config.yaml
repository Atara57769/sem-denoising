data:
  data_root: "C:\\Users\\USER\\Desktop\\workspace\\AMAT_data"
  train_sets: [1]
  val_sets: [3]
  test_set: 5
  patch_size: 64
  stride: 64

noise:
  gaussian:
    sigma: 0.10
  poisson:
    peak: 50.0
  mixed:
    sigma: 0.06
    peak: 50.0

training:
  batch_size: 16
  epochs: 1
  lr: 0.001
  seed: 42
  device: "cpu"
  checkpoint_dir: "checkpoints"

evaluation:
  output_dir: "outputs"
  results_csv: "fast_test_results.csv"
  plot_filename: "fast_benchmark_fig.png"

